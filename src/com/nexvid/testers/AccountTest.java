package com.nexvid.testers;

import com.nexvid.accounts.*;
import com.nexvid.inventory_manager.InventoryManager;
import com.nexvid.inventory_manager.Media;
import com.nexvid.inventory_manager.Renter;

import org.junit.Before;
import org.junit.Test;

import static org.junit.Assert.assertEquals;

import org.json.JSONException;
import org.json.JSONObject;

public class AccountTest
{
/*
	public AccountManager accMan;
	public InventoryManager invMan;
	public Renter rentMan;
	
	@Before
	public void init()
	{
		accMan = new AccountManager();
		invMan = new InventoryManager();
	}
	
	@Test
	public void testNewAccountMadeWithMinimalInfo()
	{
		Account testAccount= accMan.createAccount(10374281, "Bob", "Alice", "6042821836", "Alberta", "Calgary", "IDK 999", "Canada", "garbageStreet", 8978, "RegisteredCustomer", "Active", "Garbage");
		assertEquals("10374281", testAccount.getAccountID());
		assertEquals("Bob", testAccount.getFirstName());
		assertEquals("Alice", testAccount.getLastName());
		assertEquals("6042821836", testAccount.getPhoneNumber());
		assertEquals("Alberta", testAccount.getProvince());
		assertEquals("Calgary", testAccount.getCity());
		assertEquals("IDK 999", testAccount.getPostalCode());
		assertEquals("Canada", testAccount.getCountry());
		assertEquals("garbageStreet", testAccount.getStreetName());
		assertEquals(8978, testAccount.getStreetNumber());
		assertEquals("RegisteredCustomer", testAccount.getAccountType());
		assertEquals("Active", testAccount.getStatus());
		assertEquals("Garbage", testAccount.getPassword());
	}
	
	@Test
	public void testRentingAMedia() throws JSONException
	{
		JSONObject testJSON = new JSONObject();
		Account testAccount= accMan.createAccount(10374281, "Bob", "Alice", "6042821836", "Alberta", "Calgary", "IDK 999", "Canada", "garbageStreet", 8978, "RegisteredCustomer", "Active", "Garbage");
		invMan.addMovie(testJSON);
	}
	
	*/
}
